void update () {
	if (codey0.getRow() == 1 && codey0.getCol() == 1) {
		level.win();
	}
}

void init() {
	level.setTutorialText("weeeeeeeeeeeeee text goes here!\n is this a new line\n Figure out how to draw it from here");
}